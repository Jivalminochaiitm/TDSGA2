name: Email Echo Action

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v3

      - name: 24f2006969@ds.study.iitm.ac.in
        run: echo "Hello, world!"
